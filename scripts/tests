#! /usr/bin/env bash

set -e

if [[ $CI == "true" && $DRONE == "true" ]]; then
  ./scripts/install-dependencies
fi

echo "-- System information"
uname -a
env

echo "-- ./configure"
./configure

echo "-- make rts"
make rts

echo "-- make rts-editor"
make rts-editor

echo "-- make check"
make check
